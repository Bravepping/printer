file:
  # 保存文件目录
  upload-path: D:/upload/
server:
  port: 9003
spring:
#  数据库配置
  datasource:
    username: root
    url: jdbc:mysql://localhost:3306/webprint?useUnicode=true&characterEncoding=utf-8&useSSL=false&timezone=Asia/Shanghai
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  servlet:
    multipart:
      max-file-size: 512MB
      max-request-size: 512MB
  data:
#    redis配置
    redis:
      host: localhost
      port: 6379
      password: inspur123
      timeout: 5000ms
